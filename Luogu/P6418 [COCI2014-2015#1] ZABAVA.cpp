#include <cstdio>
using namespace std;
const int M = 10000005;
int dp[M], tmp[M], n, m, K, cnt[M], a[M];
int main(){
    scanf("%d %d %d", &n, &m, &K);
    for(int i = 1; i <= n; i++) scanf("%d", &a[i]), ++cnt[a[i]];
    for(int i = 1; i <= m; i++){
        for(int j = 0; j <= K; j++){
            for(int k = 0; k <= K; k++){

            }
        }
    }
}