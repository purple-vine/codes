#include <bits/stdc++.h>
using namespace std;
int main(){
    while(1){
        system("rand > 1.in");
        system("B < 1.in > 1.out");
        system("temp < 1.in > 2.out");
        if(system("fc 1.out 2.out")) return 0;
    }
}